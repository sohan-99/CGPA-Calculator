import type { Metadata, Viewport } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "CGPA Calculator - Pundra University of Science & Technology",
  description:
    "Calculate your CGPA easily with our user-friendly CGPA calculator designed specifically for Pundra University of Science & Technology students in Bangladesh.",
  keywords:
    "CGPA calculator, Pundra University, Bangladesh, GPA, grades, university calculator",
  authors: [{ name: "Pundra University" }],
};

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
